#include<iostream>
using namespace std;
int i;
insert(i)
{
    table[i]=1;
}
search(i)
{
    return table[i];
}

delete(i)
{
    table[i]=0;
}

int main()
{
    int i;
    int arr[1000];
    insert(10);
    search(10);
}